# Optional browser map keys. The local-web release supplies /plotter/; leave these empty for loopback development without providers.
VITE_PUBLIC_BASE_PATH=
VITE_MAPTILER_API_KEY=
VITE_OPENROUTESERVICE_API_KEY=
# Optional service-only provider secret. Never prefix this with VITE_.
SERPAPI_API_KEY=
